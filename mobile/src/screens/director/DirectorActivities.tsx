import ManageActivities from '../../components/ManageActivities';

/** El director de carrera gestiona las actividades academicas (Objetivo 3). */
export default function DirectorActivities() {
  return <ManageActivities activityType="academica" />;
}
