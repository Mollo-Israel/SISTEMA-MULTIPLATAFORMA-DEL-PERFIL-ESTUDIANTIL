export enum TipoActividad {
  ACADEMICA = 'ACADEMICA',
  EXTRACURRICULAR = 'EXTRACURRICULAR',
}

export enum EstadoActividad {
  BORRADOR = 'BORRADOR',
  PUBLICADA = 'PUBLICADA',
  CERRADA = 'CERRADA',
}
